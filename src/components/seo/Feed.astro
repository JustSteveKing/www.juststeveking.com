---
interface Props {
  title: string;
  href?: string;
}

const { title, href = '/rss.xml' } = Astro.props as Props;
---

<link rel="alternate" type="application/rss+xml" title={title} href={href} />
