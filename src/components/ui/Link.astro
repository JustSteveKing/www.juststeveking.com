---
import { clsx } from 'clsx';

interface Props {
  href: string;
  external?: boolean;
  underline?: boolean;
  class?: string;
}

const {
  href,
  external = href.startsWith('http') || href.startsWith('//'),
  underline = true,
  class: className,
} = Astro.props as Props;

const externalAttrs = external ? { target: '_blank', rel: 'noopener noreferrer' } : {};
---

<a
  href={href}
  class={clsx(
    'text-stone-200 transition-colors duration-200 hover:text-white',
    underline && 'underline underline-offset-4 decoration-white/20 hover:decoration-blue-300/70',
    className,
  )}
  {...externalAttrs}
>
  <slot />
  {external && <span class="sr-only">(opens in new tab)</span>}
</a>
