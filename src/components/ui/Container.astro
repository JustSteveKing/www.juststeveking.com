---
import { clsx } from 'clsx';

interface Props {
  class?: string;
}

const { class: className } = Astro.props as Props;
---

<div class={clsx('mx-auto w-full max-w-5xl px-4 sm:px-6 lg:px-8', className)}>
  <slot />
</div>
